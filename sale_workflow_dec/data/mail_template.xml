<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="sale.email_template_edi_sale" model="mail.template">
        <field name="model_id" ref="sale.model_sale_order" />
        <field
            name="subject"
        >${object.company_id.name} ${object.state in ('draft', 'sent') and (ctx.get('proforma') and 'Proforma' or 'Quotation') or 'Order'} ${object.name} (Ref ${object.client_order_ref or 'n/a' })</field>
        <field name="body_type">qweb</field>
        <field name="body_view_id" ref="view_email_template_edi_sale" />
    </record>

</odoo>
